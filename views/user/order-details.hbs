<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Details #{{orderDetails._id}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <style>
        .detail-card {
            background: #fff;
            border-radius: 12px;
            border: 1px solid #dee2e6;
            overflow: hidden;
        }
        .detail-header {
            background-color: #f8f9fa;
            padding: 20px;
            border-bottom: 1px solid #dee2e6;
        }
        .product-img {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: 4px;
            background: #eee;
        }
    </style>
</head>
<body class="bg-light">

    <div class="container mt-5 mb-5">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/">Home</a></li>
                <li class="breadcrumb-item"><a href="/orders">My Orders</a></li>
                <li class="breadcrumb-item active">Details</li>
            </ol>
        </nav>

        <div class="detail-card">
            <div class="detail-header d-md-flex justify-content-between align-items-center">
                <div>
                    <h2 class="h4 mb-1">Order Details</h2>
                    <p class="text-muted mb-0 small">ID: {{orderDetails._id}}</p>
                </div>
                <div class="mt-3 mt-md-0">
                    <span class="badge bg-success rounded-pill px-3 py-2 fs-6">
                        {{orderDetails.status}}
                    </span>
                </div>
            </div>

            <div class="p-4">
                <div class="row mb-4">
                    <div class="col-md-4 mb-3">
                        <h6 class="text-uppercase text-muted small fw-bold">Order Date</h6>
                        <p class="mb-0">{{orderDetails.date}}</p>
                    </div>
                    <div class="col-md-4 mb-3">
                        <h6 class="text-uppercase text-muted small fw-bold">Payment Method</h6>
                        <p class="mb-0 text-uppercase">{{orderDetails.paymentMethod}}</p>
                    </div>
                    <div class="col-md-4 mb-3">
                        <h6 class="text-uppercase text-muted small fw-bold">Total Amount</h6>
                        <p class="mb-0 fs-5 fw-bold text-primary">Rs. {{orderDetails.totalAmount}}</p>
                    </div>
                </div>

                <hr>

                <h5 class="mb-4 mt-2">Items Purchased</h5>
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>Item</th>
                                <th class="text-center">Quantity</th>
                                <th class="text-end">Unit Price</th>
                                <th class="text-end">Subtotal</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each products}}
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <div class="product-img me-3"></div>
                                        <div>
                                            <span class="fw-bold d-block">{{this.name}}</span>
                                            <span class="text-muted small">SKU: {{this.productId}}</span>
                                        </div>
                                    </div>
                                </td>
                                <td class="text-center">x {{this.quantity}}</td>
                                <td class="text-end text-muted">Rs. {{this.price}}</td>
                                <td class="text-end fw-bold">Rs. {{this.subtotal}}</td>
                            </tr>
                            {{/each}}
                        </tbody>
                        <tfoot>
                            <tr>
                                <td colspan="3" class="text-end fw-bold border-0">Grand Total:</td>
                                <td class="text-end fw-bold border-0 fs-5 text-primary">Rs. {{orderDetails.totalAmount}}</td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>

            <div class="card-footer bg-white p-4 border-top-0 d-flex justify-content-between">
                <a href="/orders" class="btn btn-outline-secondary">Back to My Orders</a>
                <button class="btn btn-primary" onclick="window.print()">Download Invoice</button>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
